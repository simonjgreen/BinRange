# Geometry check logs

These are generated validation logs, not physical test results. Positive catch/locating checks deliberately intersect. The runner removes its temporary meshes; printable models are only in `../exports/`.
